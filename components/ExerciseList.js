import { View, Text, FlatList } from "react-native";

import Exercise from "./Exercise";

export default function ExerciseList() {
  return (
    <View>
      <Text>ActiveWorkoutList</Text>
      {/* {exerciseList.map((element, index) => {
        return <Exercise />;
      })} */}
    </View>
  );
}
